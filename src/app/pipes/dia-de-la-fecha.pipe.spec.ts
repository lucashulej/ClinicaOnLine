import { DiaDeLaFechaPipe } from './dia-de-la-fecha.pipe';

describe('DiaDeLaFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new DiaDeLaFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
