import { EstadoPendientePipe } from './estado-pendiente-pipe';

describe('EstadoPendientePipe', () => {
  it('should create an instance', () => {
    expect(new EstadoPendientePipe()).toBeTruthy();
  });
});
