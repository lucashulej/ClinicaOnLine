import { Turnos15DiasPipe } from './turnos15-dias.pipe';

describe('Turnos15DiasPipe', () => {
  it('create an instance', () => {
    const pipe = new Turnos15DiasPipe();
    expect(pipe).toBeTruthy();
  });
});
